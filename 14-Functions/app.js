//FUNÇOES

/*
É comum ser necessário utilizar uma mesma operação em diferentes partes de nossa aplicação, diferentemente de um ciclo, trata-se de executar um determinado cálculo quando necessário, com funções isto é possivel. 
*/

function nomeDaFuncao(argumentos) {
    //código da função
}

//Para executar uma função, "chamamos" o nome dela e passamos os parâmetros. No JS uma função pode ser chamada antes de estar definida ou depois de estar definida.